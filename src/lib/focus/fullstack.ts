/**
 * TODO: Full Stack focus — reorder skills/experiences, adjust targetRole emphasis.
 * Will be enabled when niche selection + optional AI revision ship.
 */
export const fullstackFocus = {
  id: "fullstack" as const,
  label: "Full Stack",
  enabled: false,
};
